# qs-ai 配置与草稿管理入口

入口：`/operations/ai-governance/configuration`，沿用现有后台登录与组织管理员路由保护。此页通过 QS 的 `/internal/v2/interpretation/ai-workflow` 代理访问 qs-ai；组织及操作人由 QS 从认证上下文产生，浏览器不提交权限声明。

## 当前能力

- 查看 Profile、Prompt、模型路线、Schema 和评测套件的不可变版本目录，按精确标识查询、游标加载更多及读取正文。
- 从指定 Prompt 版本创建草稿，填写新模板标识、新版本及操作理由；打开已知草稿 UUID。
- 编辑系统说明、任务模板、数据前缀和允许变量，按当前修订号保存。未保存的修改不能冻结。
- 校验并冻结当前修订，显示冻结版本回执。冻结成功后当前编辑器只读；冻结不表示评测通过或生产发布。

目录代理要求 QS 资产目录版本（QS PR #98）及 AI AssetCatalog（AI PR #60）。草稿和冻结要求相应已部署接口。服务端治理开关关闭、授权拒绝或通信失败时显示不可读取，不能当作空目录。

## 结果未知与恢复

每次创建、保存、冻结都使用新命令 UUID；浏览器在发送前把原命令 UUID、草稿 UUID 和操作类型写入按当前账号分隔的 sessionStorage。记录不包含 Prompt 正文、操作理由或令牌。存储失败不发请求。

这些写请求使用 `internalV2PostOnce`，401 不自动刷新并重放。网络错误、超时、409 或不匹配的成功回执保留原命令并锁定新写入；用户只能查询原命令回执。回执暂时 404 不能证明原写入未生效。明确的 400/401/403/404/429 写请求拒绝可结束本次命令；服务端仍是最终幂等与授权边界。

页面重载会恢复待核对命令；正常成功后的草稿需自行保存 UUID 后重新打开。目前没有草稿目录，也没有未知命令人工处置入口。损坏的本地记录或持续缺失的回执需要管理员核对，不能以清空记录作为安全重试依据。重新打开草稿使用独立的 lifecycle 接口，核对明确状态、当前修订及冻结版本。未知状态、接口不可用或版本不一致不开放编辑；恢复旧创建/保存回执后也先读当前生命周期，读取失败继续保留原命令锁。冻结命令回执仍通过原查询恢复。

## 验证与剩余工作

2026-09-13：11 个相关测试套件、43 项测试通过，覆盖目录分页/旧响应隔离、原命令恢复、409/504、回执不匹配、401 禁止重放、修订与冻结及既有导航/API 回归；TypeScript 检查、全库 ESLint（零警告门槛）与生产构建通过。

通过隔离页面使用真实 React 组件及内存合成 API 完成：查看正文 → 创建 v7-preview → 修改中文模板 → 保存修订 2 → 冻结 → 编辑禁用。预览未连接生产 QS/IAM，没有真实模型调用，不是 M1–M3 业务验收。

生命周期读取已接入页面，依赖 AI PR #63 和 QS 生命周期代理部署；不回退到旧 Get 并猜测可编辑。下一步补齐草稿目录及未知命令处置入口，接入原生 Profile 注册、套件绑定、评测、审核、发布与回退页面。现有 Profile/评测页面保留旧路由；本批不切换写入权威、不关闭旧 AI 引擎。生产启用及旧入口退役须满足 qs-ai 的 M1–M5 验收计划。

2026-09-13 生命周期增量：23 项目录/草稿/API 专项测试通过；覆盖冻结后重新打开、未知/缺失/不匹配状态、服务不可用、旧回执恢复冻结状态及读取失败保留原命令锁。TypeScript、全库 ESLint 零警告检查和生产构建通过。Go/Python/MySQL 生命周期与既有管理互操作另在 qs-ai 验证，不代表真实后台权限或生产业务验收。

## 2026-09-13：原生策略版本注册

配置目录中的 Profile、Prompt 和模型路线详情可分别选择“用于注册策略版本”，在“解读策略”页签填写新版本及理由。复用策略的现有事实、输入、安全与质量规则，仅修改版本及明确选择的 Prompt/路线绑定；完整配置可展开核对。本页尚不提供任意策略规则编辑器。

注册通过 QS 的 `POST /profiles/register`，查询原命令通过 `GET /profiles/commands/{command_id}`。成功时核对原命令、配置内容与五项不可变版本引用，显示策略、Prompt 和路线版本，阻止当前已注册版本再次提交。注册成功不等于评测通过或发布；套件绑定、评测、审核与发布仍是后续步骤。

每个账号的 sessionStorage 仅保留待确认注册命令 UUID，不保存策略正文、理由或凭据。发送前写入失败则不发送；超时、409 或回执不匹配保留原命令并锁定写入；刷新后查询原回执，暂时 404 不释放锁。接口沿用不自动重放的写请求通道。

本批 5 个相关测试套件、22 项测试通过，覆盖策略规则保留、目录选择、版本引用、重复点击、原回执恢复、账号隔离及存储失败；TypeScript、全库 ESLint 和生产构建通过。隔离 React 预览使用合成 API 验证中文表单、注册成功摘要和同版本提交禁用，未连接生产 QS/IAM，不构成真实管理或 M1–M3 业务验收。

## 2026-09-13：原生评测套件绑定

配置目录的套件、策略、Prompt 和模型路线详情可以选择“用于绑定评测套件”；策略注册成功后的同名操作直接带入回执中确认的三项资产引用。填写新套件标识、版本和理由后，通过 QS `POST /suites/register` 注册；`GET /suites/commands/{command_id}` 查询原回执。

当前后端只接受已迁入的 `cross-dimension-participant-scale-v6-published` / `qs-ai-evaluation-cases/v1` 及其固定指纹作为案例来源，页面与这一契约一致；其他来源明确提示并禁止提交。注册保留原案例及质量义务，绑定新版本，不继承旧评测批准。此页没有任意案例编辑、自动评测或自动发布能力。

套件命令日志与策略日志分开并按账号隔离，仅保存 UUID。未知结果、回执缺失或目标/来源/资产引用不匹配时持续锁定，刷新后查询原命令；存储失败不发送，成功后禁用当前同版本重复注册。回执核对包含输入/输出 Schema 引用完整性，但最终授权及资产内容校验由服务端负责。

本批 5 个相关测试套件、29 项测试通过，包含目录到套件、策略回执到套件的衔接、版本和来源约束、未知结果恢复与防重放；TypeScript、全库 ESLint、生产构建通过。浏览器预览工具连续超时，本批未完成独立视觉检查；组件测试及构建不替代生产 IAM、真实评测和发布流程验收。


## 2026-09-13：原生评测准备、创建与启动

“原生评测”页签沿用现有 QS 域名和授权代理，不让浏览器直连 AI 或声明组织/操作者。目录中明确选择套件、生成路线与语义路线；套件注册成功后可带入已确认套件及生成路线，语义路线仍须另行选择。AI Prepare 解析其余固定版本，页面展示案例/候选数量、调用策略上限及完整清单；这些数字不是当前机构可用额度或实际调用量。

准备、创建与启动分开：Prepare 不写任务；用户填写目的、确认后用原 11 项引用调用 Create；显示服务端原始创建记录后，再填写启动原因、确认调用成本风险，携带当前版本调用 Start。新页面只调用 ai-workflow/evaluations 下的 prepare/create/start/Get/candidates，不调用 QS 旧 prompt-evaluations。人工审核、发布及回退操作页面尚待衔接，本批不关闭旧入口。

浏览器按账号记录 Run UUID、release fingerprint、操作阶段及已观测版本，不保存 Prompt/理由/令牌。写入日志失败不发命令；未知创建或启动保留原 Run，刷新后只读恢复，不自动重发。查询暂时 404、缺少/不匹配创建记录、版本倒退均不解锁；启动后的早期查询若仍为原 requested 版本，继续等待原结果。已确认任务可以结束查看并准备另一任务，该操作不取消服务端旧任务。切换账号会重建配置页及各操作状态。

候选列表和详情与当前任务版本绑定；版本变化提示先刷新任务，不混用旧列表和新结果。当前查询 API 通过任务 UUID 恢复查看，没有原生任务目录；模型输出以文本展示，完整来源、调用和审核证据可展开读取。旧服务缺少创建回执时仍可只读状态，但不启用 Start。

依赖：准备接口 AI #66/QS #100；创建恢复接口 AI #68/QS #101。启用前须核对两端实际部署，服务端继续是授权、版本一致性、质量门槛与执行预算的权威。此页面不单独取得公网域名：浏览器 → 现有 QS HTTPS → 内网双向 TLS → qs-ai。

验证：6 个相关测试套件、65 项测试通过，覆盖明确两步确认、原引用与版本传递、409/504 后重载恢复、早期 requested 不重复启动、原创建回执缺失/不匹配、404 不释放锁、账号隔离、日志失败、过期计划丢弃、版本下限及候选版本冲突；类型检查、全库 ESLint 零警告和生产构建通过。隔离 Chromium 预览使用真实 React 组件与内存合成 API 完成计划→创建→启动→候选读取，检查三阶段中文布局且无横向溢出；所有外部请求被阻断。该预览及测试不是实际登录、真实 IAM/模型调用或 M1–M3 业务验收。

### 2026-09-13：原生候选人工审核

- 原生评测结果页面支持逐候选选择测评语义/安全与产品职责、通过/拒绝及审核理由，展示服务器审核人、时间和理由。每个角色只能追加一次，两个职责必须由不同操作者完成；QS/AI 继续执行实际授权与唯一性规则。
- v2 门槛下可针对原始 `forbidden_claims_absent` 失败断言提交证据复核。页面从同一版本的候选、语义断言与原始语义输出中选择目标，绑定原执行 ID/输出摘要/断言详情，并要求引用候选原文。服务端继续校验全部证据，不修改原始模型结果。
- 审核使用当前 Run CAS 版本、明确确认以及不自动重放的 POST。会话按账号保存原 Run ID、配置摘要、操作种类与版本，不保存审核正文。超时/409/回执不匹配后只查询原 Run；版本未推进保持锁定，刷新页面也不能重复发送。明确拒绝后要求重新读取。
- 本批本地验证：相关 7 套测试共 86 项通过，类型检查、ESLint 和生产构建通过。覆盖确认失效、越权拒绝、存储失败、未知结果刷新恢复、版本/审核记录不匹配、两角色重复身份、语义证据绑定与原文引用。
- 使用真实 React 组件在隔离浏览器验证普通及语义复核表单、确认与提交，未发现横向溢出；合成结果与回执，不连接生产 QS、IAM 或模型，不能代替实际管理验收。
- 依赖原生评测 UI PR #25；在创建回执后端发布后合并。AI PR #68 已通过双 MySQL CI `34732562088` 并合并 `6d81565d309cdbcd8d863f802b405b858a703e5a`，主分支 CI `34733400610` 进行中。QS PR #101 部署 `34733078368` 成功，serverA API 与两份 collection 实际镜像为 `7295f40efb7e9002ff8ce5d264ab6350d04029ab` 且健康。
- 同次现场 AI 实际镜像仍为 `eb4257babb296cbab15aaf0058aa12a4a94496e7`，MySQL 迁移至 `0022_evaluation_suites`、就绪与 mTLS 探针通过；未把 AI #68 合并等同于部署。
- 下一批：最终门槛预览/确认、复审及未知调用处置、配置发布/回退/停用与命令回执恢复。M3 仍缺真实“草稿→评测→发布→生成→回退”、配置对账及旧写路径关闭；M1–M5 验收状态保持不变。

### 2026-09-13：运营端最终审核确认

- 本批仅在 qs-operating-system 接入 QS 现有 `/evaluations/{run_id}/gates` 与 `/finalize` 接口。qs-ai 继续作为纯后端负责门槛重算、权限上下文、CAS 和持久审计；QS 保留授权及转发。
- 页面展示 G1–G5、未通过原因、指标和证据复核记录。缺少人工审核时不能终结；服务端结论为通过或拒绝时均需理由和明确确认。前端不重新实现质量评分或阈值策略。
- 门槛必须绑定当前任务版本及原配置摘要。读取任务/提交审核后清除旧门槛和确认。最终审核只发送一次，超时/409/缺失或不匹配回执后保存原 Run 与版本并只读恢复；任务版本尚未推进时保持锁定。
- 最终回执核对来源版本、结果版本、配置摘要、通过/拒绝、操作理由与时间及五项门槛，展示实际确认人和理由。“审核通过”不推断配置已生效，生效状态必须另查发布记录。
- 本地 7 套相关测试 104 项通过；TypeScript、ESLint 零警告、生产构建通过。隔离浏览器使用真实组件验证通过/拒绝两条确认与回执展示流程，无横向溢出；未连接生产 QS、IAM 或模型，不构成 M3 实际验收。
- 本批依赖原生评测 PR #25 与人工审核 PR #26，按后端实际部署及前端精确版本 CI 顺序合并。后续仍需复审、未知调用处置、配置发布回退及真实业务验收。

### 2026-09-13：运营端配置发布、回退与停用

- 新增“发布与回退”页签。已审核评测可带入 Run 标识，但页面仍显式读取当前 Run、最终审核回执、原策略和精确配置范围，核对后读取当前发布指针；不会因为上一页显示审核通过就直接发布。
- 可按测评编码/版本查询精确发布范围，展示当前变更版本、发布标识、策略、生成 Prompt、模型路线和审核来源。历史采用排他版本游标；先读取历史摘要，再打开指定版本的完整前后配置及原审计，才可作为回退目标。
- 发布、回退和停用均要求明确理由及确认，发送当前 selector/version/active_publication_id，并复用不重放的 POST。服务端继续负责当前授权、完整质量门槛和 CAS；历史可读不授予回退权限。
- 浏览器按账号记录原命令 UUID、操作类型、预期配置版本及必要目标引用，不保存 Prompt、理由、模型输出或令牌。存储失败不发命令，损坏日志保持锁定；超时/409/不匹配回执只读恢复，404 不释放锁。原回执确认后清空当前状态，下一次修改必须重新读取，避免将旧回执当成最新指针。账号切换后不展示上个账号的异步结果。
- 本地相关 8 套测试共 131 项通过；TypeScript、全库 ESLint 零警告及生产构建通过。覆盖已审核来源绑定、明确确认、三种操作、原版本传递、原命令持久恢复、存储损坏/失败、404/403、重复点击和账号切换。
- 隔离 Chromium 使用真实组件和内存 API，完成准备→发布→刷新→历史详情→回退→刷新→停用，复核五个界面且无横向溢出；未访问生产 IAM/QS 或调用真实模型，不代表管理业务验收。
- 依赖 AI 历史 API PR #70（合并 c1681dbece09e3501d4582492c78b917cb1753ca）及 QS 历史代理 PR #102（合并 e1e6050f2eb6b5496cf2f268b214c2de4520d60e），两端合并后 CI/实际部署仍在跟进，页面 PR 在后端就绪前不合并。治理开关保持现状，未关闭 QS 旧管理入口。
- 用户提出的完整 Prompt 阅读/差异和多 Prompt 流程展示放在 M1–M5 之后；本页仅完成当前 M3 所需的原生管理闭环入口。M3 仍待复审、未知调用处置、实际管理操作、配置对账和旧写入口退役。

## 原生语义复审入口（2026-09-13）

- Operating 调用 QS 已有的 `POST /internal/v2/interpretation/ai-workflow/evaluations/{run_id}/reopen-review`。qs-ai 仍负责重建原始门槛、判断复审资格、确定候选和保存审计；前端不重新调用模型、不改写原输出。
- 对 v2 策略且仅 G4 必需断言未通过的已拒绝任务展示申请入口，要求理由、明确确认及当前任务版本。最多三轮；其他门槛失败、人工审核未完成、旧策略或不完整回执不开放申请。
- 历史展示原审核版本、重开后版本、候选、理由和操作者，并可展开完整原门槛及 70 条签名。核对版本顺序、候选归属、审核时间和未受影响签名；本轮候选需重新人工审核。
- 写入前保存账号隔离的最小恢复记录。409/504、网络错误或不匹配回执均保留锁，只查询原任务；版本前进本身不能确认成功，必须读到对应原版本的复审历史。存储失败不发送，明确拒绝后重新读取再操作。
- 验证：9 套相关测试 149 项通过，类型检查、全库 ESLint 零警告和生产构建通过。隔离 Chromium 使用实际组件与合成审核记录验证拒绝→确认重开→历史展开，无横向溢出，全部外部请求阻断；未连接生产 IAM 或模型，不构成实际管理验收。
- 发布历史的后端依赖已部署：AI `c1681dbece09e3501d4582492c78b917cb1753ca`、QS API `596f82f87f38eb4bb544904a88c970ce3cbf9b77`；serverA 独立核对容器健康、AI MySQL 迁移头和身份拒绝探针。Operating 发布与回退 PR #28 已合并，部署须另行核对。
- 下一批补未知调用的版本绑定明细读取，再接处置入口；当前 Get 只有未决数量及已完成处置历史，不能要求操作员猜 execution_id。真实管理闭环、配置对账和 QS 旧写入口关闭仍待验收。
