# 条件授权退役

权限总览保留角色职责、角色对比和兼岗组合。条件编辑和资源属性模式编辑已删除；旧条件授权显示“权限数据待刷新”，不会呈现为可执行权限。

重试按钮使用 /identity/me 返回的本人 permissions，按资源与动作匹配，不依赖岗位名称或 adhoc/plan 来源。缺少权限条目时不回退到角色推断。IAM 旧条件模式不能满足动作权限。

角色矩阵只用于解释和导航。QS 最终校验动作权限、机构与受试者关系、失败状态和重试资格；无结果权限者仍不得加载完整测评结果。

本版本需与 IAM、QS 及条件授权数据迁移一起切换。旧服务器不返回 permissions 时，重试按钮保持关闭。生产验收需专用运营员、计划管理员与结果评估员账号，不能只用 system 管理员证明隔离。
