version: "3.8"

services:
  qs-operating-system:
    image: ghcr.io/fangcunmount/qs-operating-system:latest
    container_name: qs-operating-system
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - infra-network

networks:
  infra-network:
    external: true
