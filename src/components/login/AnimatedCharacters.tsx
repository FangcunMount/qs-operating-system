import type React from 'react'

interface AnimatedCharactersProps {
  isTyping?: boolean
  showPassword?: boolean
  passwordLength?: number
}

const AnimatedCharacters: React.FC<AnimatedCharactersProps> = () => null

export default AnimatedCharacters
